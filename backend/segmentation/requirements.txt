flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
onnx==1.20.1
onnxruntime==1.24.1
# Prefer CPU-only PyTorch builds to keep the image small.
# Note: no "+cpu" local version here. Those wheels only exist for x86_64, so
# pinning torch==2.1.0+cpu made this file uninstallable on arm64 (Apple
# Silicon, Raspberry Pi). These versions resolve on both architectures.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
torchvision==0.27.0
ultralytics==8.3.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
supervision==0.22.0
